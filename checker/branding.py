"""Марка продукта в одном месте: расшифровка названия, команда, логотип.

Одни и те же строки нужны интерфейсу, экранам входа и сгенерированному
отчёту. Раньше они были переписаны в трёх шаблонах и расходились при правках,
поэтому теперь берутся отсюда.
"""

import base64
from pathlib import Path

APP_TITLE = 'АЛЁНА'

# Расшифровка идёт построчно: буква – слово. Иначе, читая подряд, аббревиатуру
# не собрать, а именно она объясняет название.
ACRONYM = [
    ('А', 'Автоматический'),
    ('Л', 'Ловец'),
    ('Ё', 'Ёрничества'),
    ('Н', 'Небрежности'),
    ('А', 'Аутентичности'),
]

APP_FULL_NAME = 'Автоматический Ловец Ёрничества, Небрежности и Аутентичности'
APP_TAGLINE = 'Проверка студенческих отчётов'

TEAM = '#au_team'
APP_VERSION = '1.0'

LOGO_PATH = Path(__file__).parent.parent / 'au_logo.png'

_logo_cache = None


def logo_data_uri() -> str:
    """Логотип строкой data: – отчёт остаётся одним самодостаточным файлом,
    его можно переслать почтой и открыть без сети."""
    global _logo_cache
    if _logo_cache is None:
        try:
            raw = LOGO_PATH.read_bytes()
            _logo_cache = 'data:image/png;base64,' + base64.b64encode(raw).decode()
        except OSError:
            _logo_cache = ''
    return _logo_cache
