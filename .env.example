# Copy to .env and fill in real values before running docker-compose up

# Login credentials (browser session)
AU_USERNAME=admin
AU_PASSWORD=changeme

# API key for machine-to-machine access to /api/v1 (header: X-API-Key).
# Leave empty to disable token access and allow only a logged-in session.
#   python -c "import secrets; print(secrets.token_hex(24))"
AU_API_KEY=

# Flask session secret — use a long random string in production:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-string

# Host port for the web UI (default 5000)
PORT=5000

# PostgreSQL. docker compose starts a `db` service from POSTGRES_* below and
# points the app at it via DATABASE_URL. Keep the credentials in sync.
# Leave DATABASE_URL empty (or unset) to fall back to the local JSON store.
POSTGRES_USER=au
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=au_reports
DATABASE_URL=postgresql://au:change-this-password@db:5432/au_reports
